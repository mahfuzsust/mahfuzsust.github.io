<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Min Stack</title>
      <link href="/2021/05/24/min-stack/"/>
      <url>/2021/05/24/min-stack/</url>
      
        <content type="html"><![CDATA[<p>Design a stack that supports push, pop, top, and retrieving the minimum element in constant time.</p><p>Implement the <code>MinStack</code> class:</p><ul><li><code>MinStack()</code> initializes the stack object.</li><li><code>void push(val)</code> pushes the element <code>val</code> onto the stack.</li><li><code>void pop()</code> removes the element on the top of the stack.</li><li><code>int top()</code> gets the top element of the stack.</li><li><code>int getMin()</code> retrieves the minimum element in the stack.</li></ul><p><strong>Example 1:</strong></p><pre><code>Input[&quot;MinStack&quot;,&quot;push&quot;,&quot;push&quot;,&quot;push&quot;,&quot;getMin&quot;,&quot;pop&quot;,&quot;top&quot;,&quot;getMin&quot;][[],[-2],[0],[-3],[],[],[],[]]Output[null,null,null,null,-3,null,0,-2]ExplanationMinStack minStack = new MinStack();minStack.push(-2);minStack.push(0);minStack.push(-3);minStack.getMin(); // return -3minStack.pop();minStack.top();    // return 0minStack.getMin(); // return -2</code></pre><p><strong>Solution:</strong></p><pre><code>class MinStack &#123;    private List&lt;Integer&gt; values = null;    private List&lt;Integer&gt; mins = null;        public MinStack() &#123;        this.values = new ArrayList&lt;Integer&gt;();        this.mins = new ArrayList&lt;Integer&gt;();    &#125;        private void setMin(int val) &#123;        if(this.mins.isEmpty() || val &lt;= this.getMin()) &#123;            this.mins.add(val);        &#125;     &#125;        public void push(int val) &#123;        this.values.add(val);        setMin(val);    &#125;        public void pop() &#123;        if(this.values.remove(this.values.size() - 1) == this.getMin()) &#123;            this.mins.remove(this.mins.size() - 1);        &#125;    &#125;        public int top() &#123;        return this.values.get(this.values.size() - 1);    &#125;        public int getMin() &#123;        return this.mins.get(this.mins.size() - 1);    &#125;&#125;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> algorithm </tag>
            
            <tag> java </tag>
            
            <tag> stack </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Convert Sorted Array to Binary Search Tree</title>
      <link href="/2021/05/23/convert-sorted-array-to-binary-search-tree/"/>
      <url>/2021/05/23/convert-sorted-array-to-binary-search-tree/</url>
      
        <content type="html"><![CDATA[<p>Given an integer array <code>nums</code> where the elements are sorted in <strong>ascending order</strong>, convert <em>it to a <strong>height-balanced</strong> binary search tree</em>.</p><p>A <strong>height-balanced</strong> binary tree is a binary tree in which the depth of the two subtrees of every node never differs by more than one.</p><p><strong>Example 1:</strong><br><img src="https://assets.leetcode.com/uploads/2021/02/18/btree1.jpg"><br>    Input: nums = [-10,-3,0,5,9]<br>    Output: [0,-3,9,-10,null,5]<br>    Explanation: [0,-10,5,null,-3,null,9] is also accepted:</p><p><img src="https://assets.leetcode.com/uploads/2021/02/18/btree2.jpg"><br><strong>Example 2:</strong><br><img src="https://assets.leetcode.com/uploads/2021/02/18/btree.jpg"><br>    Input: nums = [1,3]<br>    Output: [3,1]<br>    Explanation: [1,3] and [3,1] are both a height-balanced BSTs.</p><p><strong>Constraints:</strong></p><ul><li><code>1 &lt;= nums.length &lt;= 104</code></li><li><code>-104 &lt;= nums[i] &lt;= 104</code></li><li><code>nums</code> is sorted in a <strong>strictly increasing</strong> order.</li></ul><p><strong>Solution</strong></p><pre><code>public class TreeNode &#123;     int val;     TreeNode left;     TreeNode right;     TreeNode() &#123;&#125;     TreeNode(int val) &#123; this.val = val; &#125;     TreeNode(int val, TreeNode left, TreeNode right) &#123;         this.val = val;         this.left = left;         this.right = right;     &#125;&#125;class Solution &#123;    private TreeNode sortedArrayToBST(int[] nums, int p, int q) &#123;        if(p &gt; q) return null;        int mid = (q + p) / 2;        TreeNode node = new TreeNode(nums[mid]);        node.left = sortedArrayToBST(nums,p, mid -1);        node.right = sortedArrayToBST(nums, mid + 1, q);        return node;    &#125;    public TreeNode sortedArrayToBST(int[] nums) &#123;        return sortedArrayToBST(nums, 0, nums.length - 1);           &#125;&#125;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> algorithm </tag>
            
            <tag> java </tag>
            
            <tag> binary-search-tree </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Fibonacci Number</title>
      <link href="/2021/05/23/fibonacci-number/"/>
      <url>/2021/05/23/fibonacci-number/</url>
      
        <content type="html"><![CDATA[<p>The <strong>Fibonacci numbers</strong>, commonly denoted <code>F(n)</code> form a sequence, called the <strong>Fibonacci sequence</strong>, such that each number is the sum of the two preceding ones, starting from <code>0</code> and <code>1</code>. That is,</p><pre><code>F(0) = 0, F(1) = 1 F(n) = F(n - 1) + F(n - 2), for n &gt; 1.</code></pre><p>Given <code>n</code>, calculate <code>F(n)</code>.</p><pre><code>Example 1:Input: n = 2 Output: 1 Explanation: F(2) = F(1) + F(0) = 1 + 0 = 1.Example 2:Input: n = 3 Output: 2 Explanation: F(3) = F(2) + F(1) = 1 + 1 = 2.Example 3:Input: n = 4 Output: 3 Explanation: F(4) = F(3) + F(2) = 2 + 1 = 3.</code></pre><p>Solution</p><pre><code>class Solution &#123;    private int[] val = new int[31];    private int getVal(int n) &#123;        if(val[n] == 0) &#123;            val[n] = fib(n);        &#125;        return val[n];    &#125;    public int fib(int n) &#123;        if(n &lt;= 0) return 0;        if(n == 1) return 1;        return getVal(n-1) + getVal(n-2);    &#125;&#125;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> fibonacci </tag>
            
            <tag> algorithm </tag>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Climbing Stairs</title>
      <link href="/2021/05/23/climbing-stairs/"/>
      <url>/2021/05/23/climbing-stairs/</url>
      
        <content type="html"><![CDATA[<p>Each time you can either climb <code>1</code> or <code>2</code> steps. In how many distinct ways can you climb to the top?</p><pre><code>Example 1:Input: n = 2Output: 2Explanation: There are two ways to climb to the top.1. 1 step + 1 step2. 2 stepsExample 2:Input: n = 3Output: 3Explanation: There are three ways to climb to the top.1. 1 step + 1 step + 1 step2. 1 step + 2 steps3. 2 steps + 1 step</code></pre><p>Solution:</p><p>This is a good example of fibonacci implementation.</p><pre><code>class Solution &#123;    private int[] val = new int[46];    public int getValue(int n) &#123;        if(n &lt; 0) return 0;        if(val[n] == 0) &#123;            val[n] = climbStairs(n);        &#125;        return val[n];    &#125;        public int climbStairs(int n) &#123;        if(n &lt;= 0) return 0;        if(n == 1) return 1;        if(n == 2) return 2;        return (getValue(n-1) + getValue(n-2));    &#125;&#125;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> fibonacci </tag>
            
            <tag> algorithm </tag>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Log everyday in google spreadsheet</title>
      <link href="/2021/03/24/log-everyday-in-google-spreadsheet/"/>
      <url>/2021/03/24/log-everyday-in-google-spreadsheet/</url>
      
        <content type="html"><![CDATA[<p>I just wanted to create a daily task list with notes and reminders in google spreadsheet. So, I created a template which will look like this.<br><img src="/content/images/2021/03/Screen-Shot-2021-03-24-at-9.44.06-AM.png"><br>Now, I need to create a new sheet everyday where the sheet name will denote the day and after each month I needed it to back up in my google drive.</p><p>To solve this problem, I have created a app script which will create a new sheet everyday I open the spreadsheet and backup in a separate file.</p><p>My tasks will be</p><ul><li>Create a new clone of the template everyday</li><li>Backup current sheet monthly</li><li>Create google drive folders to organize</li></ul><p>First, we need to create a clone of the template sheet.</p><pre><code>function cloneTemplateDaily() &#123;  var name = Utilities.formatDate(new Date(), &quot;GMT&quot;, &quot;dd-MM-yyyy&quot;);    var ss = SpreadsheetApp.getActiveSpreadsheet();  var duplicate_sheet = ss.getSheetByName(name);   if(duplicate_sheet == null) &#123;     var templateSheet = ss.getSheetByName(&quot;template&quot;);    ss.setActiveSheet(templateSheet);    duplicate_sheet = ss.duplicateActiveSheet();    duplicate_sheet.setName(name);  &#125;  ss.setActiveSheet(duplicate_sheet);&#125;</code></pre><p>Then I need to backup the sheet to my google drive</p><pre><code>function backupSheetMonthly() &#123;  var lastMonthDate = getLastMonthDate();  var fileName = Utilities.formatDate(lastMonthDate, &quot;GMT&quot;, &quot;MMMM, yyyy&quot;);  var year = Utilities.formatDate(lastMonthDate, &quot;GMT&quot;, &quot;yyyy&quot;);  var rootFolder = getFolder(&quot;My Logs&quot;);  var yearFolder = getFolder(year, rootFolder);  if(!fileExist(fileName, yearFolder)) &#123;    var file = DriveApp.getFileById(SpreadsheetApp.getActiveSpreadsheet().getId())    file.makeCopy(fileName, yearFolder);    deleteSheets();  &#125;&#125;</code></pre><p>Now, all the methods that I require</p><pre><code>function fileExist(name, folder) &#123;  var files = folder.getFilesByName(name);  while(files.hasNext())&#123;    var file = files.next();    if(file.getName() == month) &#123;      return true;    &#125;  &#125;;  return false;&#125;function getFolder(name, parent) &#123;  var folders = null;  if(!parent) &#123;    folders = DriveApp.getFoldersByName(name);  &#125;  else &#123;    folders = parent.getFoldersByName(name);  &#125;    while(folders.hasNext())&#123;    var folder = folders.next();    if(folder.getName() == name) &#123;      return folder;    &#125;  &#125;  if(!parent) &#123;    return DriveApp.createFolder(name);  &#125;  return parent.createFolder(name);&#125;function getLastMonthDate() &#123;  var dt = new Date();  dt.setDate(0);  dt.setDate(1);  return dt;&#125;function deleteSheets() &#123;  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheets = ss.getSheets();  for(var i = 0; i &lt; sheets.length; i++) &#123;    if(sheets[i].getName() !== &quot;template&quot;) &#123;      ss.setActiveSheet(sheets[i]);      ss.deleteActiveSheet();      Utilities.sleep(100);    &#125;  &#125;&#125;</code></pre><p>Lastly, I created two separate trigger that will call my two functions</p><ol><li>cloneTemplateDaily</li><li>backupSheetMonthly</li></ol><p>I set up a trigger poninted to From spreadsheet - On open cloneTemplateDaily so there will be a new sheet name current date with the same template</p><p>And a time based trigger that will run 1st of every month to run backupSheetMonthly function.</p>]]></content>
      
      
      
        <tags>
            
            <tag> app script </tag>
            
            <tag> spreadsheet </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vim configuration</title>
      <link href="/2021/03/13/vim-configuration/"/>
      <url>/2021/03/13/vim-configuration/</url>
      
        <content type="html"><![CDATA[<p>I’m going through some vim configuration to make it feel simpler and easier to use.</p><h2 id="Nerdtree-configuration"><a href="#Nerdtree-configuration" class="headerlink" title="Nerdtree configuration"></a>Nerdtree configuration</h2><h3 id="Toggle-nerdtree-with-sync-to-open-file"><a href="#Toggle-nerdtree-with-sync-to-open-file" class="headerlink" title="Toggle nerdtree with sync to open file"></a>Toggle nerdtree with sync to open file</h3><pre><code>&quot; map nerdtree to the ctrl+nfunction MyNerdToggle()    if &amp;filetype == &#39;nerdtree&#39; || exists(&quot;g:NERDTree&quot;) &amp;&amp; g:NERDTree.IsOpen()        :NERDTreeToggle    else        :NERDTreeFind    endifendfunctionnnoremap &lt;C-n&gt; :call MyNerdToggle()&lt;CR&gt;</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> vim </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Kafka server setup</title>
      <link href="/2021/03/08/kafka-server-setup/"/>
      <url>/2021/03/08/kafka-server-setup/</url>
      
        <content type="html"><![CDATA[<p>Script to start a kafka server</p><pre><code>#!/bin/shsetProperty()&#123;    awk -v pat=&quot;^$1=&quot; -v value=&quot;$1=$2&quot; &#39;&#123; if ($0 ~ pat) print value; else print $0; &#125;&#39; $3 &gt; $3.tmp    mv $3.tmp $3&#125;downloadAndPrepare() &#123;    DIR=&quot;kafka/&quot;    if [[ -d &quot;$DIR&quot; ]]; then        cd kafka        rm -rf nohup.out    else        wget https://www-us.apache.org/dist/kafka/2.3.0/kafka_2.12-2.3.0.tgz        tar -xzf kafka_2.12-2.3.0.tgz        mv kafka_2.12-2.3.0 kafka        cd kafka        rm -rf kafka_2.12-2.3.0.tgz    fi&#125;stopServer() &#123;    PIDS=$(ps ax | grep -i &#39;kafka\.Kafka&#39; | grep java | grep -v grep | awk &#39;&#123;print $1&#125;&#39;)    if [[ -z &quot;$PIDS&quot; ]]; then        echo &quot;No kafka server to stop&quot;    else        for pid in $PIDS; do kill -9 $pid; done    fi    PIDS=$(ps ax | grep java | grep -i QuorumPeerMain | grep -v grep | awk &#39;&#123;print $1&#125;&#39;)    if [[ -z &quot;$PIDS&quot; ]]; then        echo &quot;No zookeeper server to stop&quot;    else        for pid in $PIDS; do kill -9 $pid; done    fi&#125;startServer() &#123;    nohup bin/zookeeper-server-start.sh config/zookeeper.properties &amp;    nohup bin/kafka-server-start.sh config/server.properties &amp;&#125;helpFunction()&#123;   echo &quot;&quot;   echo &quot;Usage: $0 -n number&quot;   echo -e &quot;\t-n Number of kafka server to launch&quot;   exit 1&#125;while getopts &quot;n:&quot; optdo   case &quot;$opt&quot; in      n ) number=&quot;$OPTARG&quot; ;;      ? ) helpFunction ;;   esacdoneif [[ -z &quot;$number&quot; ]] || ! [[ &quot;$number&quot; =~ ^[0-9]+$ ]]then   helpFunctionfidownloadAndPreparestopServerstartServercounter=1while [[ $&#123;counter&#125; -lt $&#123;number&#125; ]]do    cp config/server.properties config/&quot;server-$counter.properties&quot;    setProperty &quot;broker.id&quot; &quot;$counter&quot; config/&quot;server-$counter.properties&quot;    setProperty &quot;log.dirs&quot; &quot;/tmp/kafka-logs-$counter&quot; config/&quot;server-$counter.properties&quot;    echo &quot;listeners=PLAINTEXT://:$((counter+9092))&quot; &gt;&gt; config/&quot;server-$counter.properties&quot;    nohup bin/kafka-server-start.sh config/&quot;server-$counter.properties&quot; &amp;    counter=$(( counter+1 ))done</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> kafka </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>How to implement cache in Java</title>
      <link href="/2020/08/25/how-to-implement-cache-in-java/"/>
      <url>/2020/08/25/how-to-implement-cache-in-java/</url>
      
        <content type="html"><![CDATA[<p>According to <a href="https://en.wikipedia.org/wiki/Cache_%28computing%29">wikipedia</a>, a cache is a hardware or software component that stores data so that future requests for that data can be served faster.</p><h4 id="Benefits"><a href="#Benefits" class="headerlink" title="Benefits:"></a>Benefits:</h4><ul><li>Faster access of data in O(1)</li><li>Computation complexity once for the first time</li></ul><h4 id="Types"><a href="#Types" class="headerlink" title="Types:"></a>Types:</h4><ul><li>Memory cache</li><li>Database cache</li><li>Disk cache, etc</li></ul><p>In this post, we’ll go through the steps to create a in memory cache in java.</p><p>To create a cache, we can simply use a map / dictionary data structure and we can get the expected result of O(1) for both get and put operation.</p><p>But, we can’t store everything in our cache. We have storage and performance limits.</p><p>A cache eviction algorithm is a way of deciding which element to evict when the cache is full. To gain optimized benefits there are many algorithms for different use cases.</p><ul><li>Least Recently Used (LRU)</li><li>Least Frequently Used (LFU)</li><li>First In First Out (FIFO)</li><li>Last In First Out (LIFO) etc.</li></ul><blockquote><p>There are only two hard things in computer science, Cache invalidation and naming things. — Phil Karlton</p></blockquote><p>In our design, we will use</p><ul><li><em>HashMap</em> to get and put data in O(1)</li><li>Doubly linked list</li></ul><p><img src="https://cdn-images-1.medium.com/max/800/1*enbSTt8WK0VFPxMCu9nJHw.png">Cache implementation strategy<br>We are using doubly linked list to determine which key to delete and have the benefit of adding and deleting keys in O(1).</p><p>Initially we will declare a model to store our key-value pair, hit count and reference node to point previous and next node.</p><pre><code>public class CacheItem&lt;K, V&gt; &#123;    private K key;        private V value;        private int hitCount = 0; // LFU require this        private CacheItem prev, next;            public CacheItem(K key, V value) &#123;                this.value = value;                        this.key = key;        &#125;            public void incrementHitCount() &#123;                this.hitCount++;        &#125;        // getter / setter&#125;</code></pre><p>Now, we can outline our cache class with basic functionalities of get, put, delete and size.</p><pre><code>public class Cache&lt;K, V&gt; &#123;    private Map&lt;K, CacheItem&gt; map;    private CacheItem first, last;    private int size;    private final int CAPACITY;    private int hitCount = 0;    private int missCount = 0;    public Cache(int capacity) &#123;        CAPACITY = capacity;  //         map = new HashMap&lt;&gt;(CAPACITY);    &#125;        public void put(K key, V value) &#123;&#125;    public V get(K key) &#123;&#125;    public void delete(K key) &#123;        deleteNode(map.get(key));    &#125;    public int size() &#123;        return size;    &#125;    public int getHitCount() &#123;        return hitCount;    &#125;    public int getMissCount() &#123;        return missCount;    &#125;&#125;</code></pre><p>We also included hit count and miss count to determine the performance of our cache.</p><p>Hit ratio = total hit / (total hit + total miss)</p><p>For our put method</p><ol><li><p>If key already exists, update the value</p></li><li><p>Otherwise, If size exceeds capacity</p></li><li><p>Delete existing node using appropriate strategy</p></li><li><p>Add new node in the top</p><p> public void put(K key, V value) {</p><pre><code> CacheItem node = new CacheItem(key, value); if(map.containsKey(key) == false) &#123;     if(size() &gt;= CAPACITY) &#123;         deleteNode(first);             &#125;             addNodeToLast(node);     &#125;     map.put(key, node);</code></pre><p> }</p></li></ol><p>In our delete method</p><ul><li><p>Remove node from map</p></li><li><p>Delete all references associated with that node.</p><p>  private void deleteNode(CacheItem node) {    </p><pre><code>  if(node == null) &#123;              return;      &#125;      if(last == node) &#123;              last = node.getPrev();      &#125;      if(first == node) &#123;              first = node.getNext();      &#125;      map.remove(node.getKey());      node = null; // Optional, collected by GC      size--;</code></pre><p>  }</p></li></ul><p>We can add node in the top. The last pointer will reference to the last inserted node.</p><pre><code>private void addNodeToLast(CacheItem node) &#123;    if(last != null) &#123;        last.setNext(node);        node.setPrev(last);    &#125;    last = node;    if(first == null) &#123;        first = node;    &#125;    size++;&#125;</code></pre><p>In our get method,</p><ul><li><p>Get data from map</p></li><li><p>Increment the counter for that item. ( Useful for lease frequently used )</p></li><li><p>Reorder the linked list.</p><p>  public V get(K key) {</p><pre><code>  if(map.containsKey(key) == false) &#123;      return null;  &#125;  CacheItem node = (CacheItem) map.get(key);  node.incrementHitCount();  reorder(node);  return (V) node.getValue();</code></pre><p>  }</p></li></ul><p>Reorder is the key for this implementation. For different algorithm this reorder to delete method will change.</p><h4 id="Least-Recently-Used-LRU"><a href="#Least-Recently-Used-LRU" class="headerlink" title="Least Recently Used (LRU)"></a>Least Recently Used (LRU)</h4><p>Delete candidate is the oldest used entry.</p><ul><li><p>The latest accessed node will be at the last end along with newly added items. In this way, we can delete from the first easily.</p><p>  private void reorder(CacheItem node) {</p><pre><code>  if(last == node) &#123;      return;  &#125;  if(first == node) &#123;      first = node.getNext();  &#125; else &#123;      node.getPrev().setNext(node.getNext());  &#125;  last.setNext(node);  node.setPrev(last);  node.setNext(null);  last = node;</code></pre><p>  }</p></li></ul><h4 id="Least-Frequently-Used-LFU"><a href="#Least-Frequently-Used-LFU" class="headerlink" title="Least Frequently Used (LFU)"></a>Least Frequently Used (LFU)</h4><p>Delete candidate is the least accessed entry.</p><p>We have to sort items based on the frequency the nodes being accessed.</p><p>To avoid getting deleted, for each accessed items needs to reach top based on their frequency.</p><ul><li><p>Iterated a loop, which swaps the node if the frequency is greater than it’s next node frequency.</p><p>  private void reorder(CacheItem node) {</p><pre><code>  if(last == node) &#123;      return;  &#125;  CacheItem nextNode = node.getNext();  while (nextNode != null) &#123;      if(nextNode.getHitCount() &gt; node.getHitCount()) &#123;          break;      &#125;      if(first == node) &#123;          first = nextNode;      &#125;      if(node.getPrev() != null) &#123;          node.getPrev().setNext(nextNode);      &#125;      nextNode.setPrev(node.getPrev());      node.setPrev(nextNode);      node.setNext(nextNode.getNext());      if(nextNode.getNext() != null) &#123;          nextNode.getNext().setPrev(node);      &#125;      nextNode.setNext(node);      nextNode = node.getNext();  &#125;  if(node.getNext() == null) &#123;      last = node;  &#125;</code></pre><p>  }</p></li></ul><p>This way, there is an edge case where, after adding items that require delete from the start can cause deletion of the high frequency node.</p><p>To solve this issue, we need to add nodes pointing our first node for least frequently accessed.</p><pre><code>private void addNodeToFirst(CacheItem node) &#123;    if(first != null) &#123;        node.setNext(first);        first.setPrev(node);    &#125;    first = node;    if(last == null) &#123;        last = node;    &#125;    size++;&#125;</code></pre><h4 id="First-In-First-Out-FIFO"><a href="#First-In-First-Out-FIFO" class="headerlink" title="First In First Out (FIFO)"></a>First In First Out (FIFO)</h4><p>Delete candidate is the first node.</p><ul><li>To add node we will use <em>addNodeToLast</em></li><li>Reordering is not required.</li><li>Delete node from first</li></ul><h4 id="Last-In-First-Out-LIFO"><a href="#Last-In-First-Out-LIFO" class="headerlink" title="Last In First Out (LIFO)"></a>Last In First Out (LIFO)</h4><p>Delete candidate is the last node.</p><ul><li>To add node we will use <em>addNodeToLast</em></li><li>Reordering is not required.</li><li>Delete node from last</li></ul><h3 id="Questions"><a href="#Questions" class="headerlink" title="Questions:"></a>Questions:</h3><ol><li>We can use array /ArrayList to solve the same issue with simplified coding.</li></ol><p>Yes, it would be simpler. The aspects for not using,</p><ul><li>ArrayList has complexity of insertion and deletion of O(n) in worst case</li><li>To search from there list, the worst case complexity would be O(n)</li></ul><ol start="2"><li>We could have used the LinkedList instead of ArrayList.</li></ol><p>Once again, yes. LinkedList had the benefit for adding and removing node at O(1). The aspects for not using,</p><ul><li>We will still face the searching complexity of O(n)</li></ul><ol start="3"><li>For LFU, we are having a complexity of O(n) in reordering method.</li></ol><p>Yes, unfortunately.</p><ul><li>I’m reordering here after each get request, which increases the complexity for each get. For post heavy system, this approach will benefit.</li><li>It can be done at the moment of deletion. This way the complexity will increase at the time of putting data. For get heavy system, this approach will benefit more.</li></ul><p>I have came up a solution with this so far. Please suggest any better idea.</p><ol start="4"><li>We are using HashMap. It can have the issue of hash collision. Then the performance will not be O(1) as mentioned.</li></ol><p>Absolutely.</p><ul><li>It’s always recommended to use better hash function that cause less collision.</li><li>Make sure our load factor remains 0.75 or less.</li><li>If the load factor is more than default or provided one, from Java 8 HashMap automatically increase the size and rehash the full structure.</li><li>From Java 8, the collision also being handled by using balanced tree to obtain O(log n) performance.</li></ul><p>I’ve written a post with implementation and explanation <a href="https://medium.com/swlh/hashmap-implementation-for-java-90a5f58d4a5b">HashMap Implementation for Java</a>.</p><ol start="5"><li>Is this implementation thread safe?</li></ol><p>No. To be thread safe</p><ul><li>We can use <em>ConcurrentHashMap</em> instead of <em>HashMap</em></li><li>Use synchronized block</li></ul><ol start="6"><li>How to add time based eviction strategy to auto delete items?</li></ol><p>No. To implement this, we need to run a timer thread to define which items are eligible for eviction and perform deletion operation.</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> cache </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Find pair of numbers in an array with a given sum</title>
      <link href="/2020/08/21/find-pair-of-numbers-in-an-array-with-a-given-sum/"/>
      <url>/2020/08/21/find-pair-of-numbers-in-an-array-with-a-given-sum/</url>
      
        <content type="html"><![CDATA[<p>There are multiple ways to find the pair of numbers in a given array. The numbers in the array can be in two ways.</p><ol><li>Sorted</li><li>Unsorted</li></ol><p>If the numbers are sorted then we can get easily with the complexity of O(n). The brute force approach will be easily applicable for both the sorted and unsorted array of numbers.</p><h3 id="Brute-force"><a href="#Brute-force" class="headerlink" title="Brute force:"></a>Brute force:</h3><p>Brute force approach will check each and every number to determine if it equals to the given sum. The complexity of this approach is O(n²).</p><pre><code>public void findPair(int[] array, int sum) &#123;    for(int i = 0; i &lt; array.length - 1; i++) &#123;        for(int j = i + 1; j &lt; array.length; j++) &#123;            if(array[i] + array[j] == sum) &#123;                // The pair is &#123; array[i], array[j] &#125;            &#125;        &#125;    &#125;&#125;</code></pre><h3 id="Sorted"><a href="#Sorted" class="headerlink" title="Sorted:"></a>Sorted:</h3><p>In this approach, we need to sort the unsorted array(if needed). In this case, the complexity depends on the complexity of the sorting algorithm. If we choose a sorting algorithm of complexity O(n) we can solve this problem in O(n) complexity.</p><pre><code>public void findPair(int[] array, int sum) &#123;    // any sort method can be used.    Arrays.sort(array);        int i = 0, j = array.length - 1;        while(i &lt; j) &#123;        int pairSum = array[i] + array[j];        if(pairSum == sum) &#123;            // The pair is &#123; array[i], array[j] &#125;            // if we want to continue i++ otherwise break        &#125; else if(pairSum &gt; sum) &#123;            j--;        &#125; else &#123;            i++;        &#125;    &#125;&#125;</code></pre><h3 id="Hash-Flag"><a href="#Hash-Flag" class="headerlink" title="Hash / Flag:"></a>Hash / Flag:</h3><p>In this approach, we can get the pairs in O(n) complexity. We can use hashmap or flag array to set counter for each value and check using negation from the given sum.</p><pre><code>public void findPair(int[] array, int sum) &#123;    Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();    for (int i = 0; i &lt; array.length; i++) &#123;        map.put(array[i], map.getOrDefault(array[i], 0) + 1);    &#125;    for (int i = 0; i &lt; array.length; i++) &#123;        if(map.containsKey(sum - array[i])) &#123;            // The pair is &#123; array[i], sum - array[i] &#125;        &#125;    &#125;&#125;</code></pre><p>In this way, we will get the pairs twice. Because in each occurrence it will check with the hashmap and make a pair. We can add a set to get unique pairs.</p><h3 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics:"></a>Metrics:</h3><p>I tried to run these to get some metrics. Sharing those might help to get some understanding.<br>100001000001000000Brute force22ms1453msToo longSorted O(n log(n))4ms20ms293msHashmap8ms44ms276ms</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> array </tag>
            
            <tag> number theory </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>HashMap Implementation for Java</title>
      <link href="/2020/08/18/hashmap-implementation-for-java/"/>
      <url>/2020/08/18/hashmap-implementation-for-java/</url>
      
        <content type="html"><![CDATA[<p>HashMap is a dictionary data structure provided by java. It’s a Map-based collection class that is used to store data in Key &amp; Value pairs. In this article, we’ll be creating our own hashmap implementation.</p><p>The benefit of using this data structure is faster data retrieval. It has data access complexity of O(1) in the best case.<br><img src="/content/images/2021/03/1.png" alt="Image for post"><br>In layman’s terms, a for each key we get the associated value.</p><p>To implement this structure,</p><ol><li>We need a list to store all the keys</li><li>Key — Value relationship to get value based on key</li></ol><p>We can have a list containing all the key, values and to access we need to search all of it.</p><p>But the main point of hash map is to access the keys faster in 0(1) access time.</p><p>Here, hashing comes into play. We can hash the key and relate it with the index to retrieve data faster.</p><p>Hash comes with a problem too, collision. It is always recommended to use a better hash function that can reduce chances of collision.<br><img src="/content/images/2021/03/2.png" alt="Image for post"><br>Multiple hash can have same hash key. For that reason, there is a bucket or container for each key where all the values are store if collision occurs.</p><p>Let’s dive into a basic implementation of our hashmap.</p><p>Firstly, we need an array to store all the keys, a bucket model to store all the entry and a wrapper for our key, value pair.</p><pre><code>public class MyKeyValueEntry&lt;K, V&gt; &#123;    private K key;    private V value;    public MyKeyValueEntry(K key, V value) &#123;        this.key = key;        this.value = value;    &#125;    // getters &amp; setters    // hashCode &amp; equals&#125;</code></pre><p>Bucket to store all the key values</p><pre><code>public class MyMapBucket &#123;    private List&lt;MyKeyValueEntry&gt; entries;    public MyMapBucket() &#123;        if(entries == null) &#123;            entries = new LinkedList&lt;&gt;();        &#125;    &#125;    public List&lt;MyKeyValueEntry&gt; getEntries() &#123;        return entries;    &#125;    public void addEntry(MyKeyValueEntry entry) &#123;        this.entries.add(entry);    &#125;    public void removeEntry(MyKeyValueEntry entry) &#123;        this.entries.remove(entry);    &#125;&#125;</code></pre><p>Lastly, implementation of our hashmap</p><pre><code>public class MyHashMap&lt;K, V&gt; &#123;    private int CAPACITY = 10;    private MyMapBucket[] bucket;    private int size = 0;    public MyHashMap() &#123;        this.bucket = new MyMapBucket[CAPACITY];    &#125;    private int getHash(K key) &#123;        return (key.hashCode() &amp; 0xfffffff) % CAPACITY;    &#125;    private MyKeyValueEntry getEntry(K key) &#123;        int hash = getHash(key);        for (int i = 0; i &lt; bucket[hash].getEntries().size(); i++) &#123;            MyKeyValueEntry myKeyValueEntry = bucket[hash].getEntries().get(i);            if(myKeyValueEntry.getKey().equals(key)) &#123;                return myKeyValueEntry;            &#125;        &#125;        return null;    &#125;    public void put(K key, V value) &#123;        if(containsKey(key)) &#123;            MyKeyValueEntry entry = getEntry(key);            entry.setValue(value);        &#125; else &#123;            int hash = getHash(key);            if(bucket[hash] == null) &#123;                bucket[hash] = new MyMapBucket();            &#125;            bucket[hash].addEntry(new MyKeyValueEntry&lt;&gt;(key, value));            size++;        &#125;    &#125;    public V get(K key) &#123;        return containsKey(key) ? (V) getEntry(key).getValue() : null;    &#125;    public boolean containsKey(K key) &#123;        int hash = getHash(key);        return !(Objects.isNull(bucket[hash]) || Objects.isNull(getEntry(key)));    &#125;    public void delete(K key) &#123;        if(containsKey(key)) &#123;            int hash = getHash(key);            bucket[hash].removeEntry(getEntry(key));            size--;        &#125;    &#125;    public int size() &#123;        return size;    &#125;&#125;</code></pre><p><strong>Put into map:</strong></p><ol><li>If key already exists, then update value of that key.</li><li>Otherwise, add an entry to the bucket.</li></ol><p><strong>Get from map:</strong></p><ol><li>Check if the key exists, and return data.</li></ol><p><strong>Contains:</strong></p><ol><li>Check if the bucket is null</li><li>If not, then the bucket contains the key.</li></ol><p><strong>Performance:</strong></p><p>It has the performance of O(1) in best case and O(n) in worst case.</p><p><strong>Java Improvement:</strong></p><ol><li>From Java version 8, All of the hashing based Map implementations: HashMap, Hashtable, LinkedHashMap, WeakHashMap and ConcurrentHashMap are modified to use an enhanced hashing algorithm for string keys when the capacity of the hash table has ever grown beyond 512 entries. The enhanced hashing implementation uses the murmur3 hashing algorithm along with random hash seeds and index masks. These enhancements mitigate cases where colliding String hash values could result in a performance bottleneck. <a href="https://hg.openjdk.java.net/jdk8/jdk8/jdk/rev/43bd5ee0205e">Alternative String hashing implementation</a></li><li>From Java version 8, once the number of items in a hash bucket grows beyond a certain threshold, that bucket will switch from using a linked list of entries to a balanced tree. In the case of high hash collisions, this will improve worst-case performance from O(n) to O(log n). <a href="https://openjdk.java.net/jeps/180">Handle Frequent HashMap Collisions with Balanced Trees</a>.</li></ol><p><strong>Java Hashmap features:</strong></p><ul><li><p>The default initial capacity is 16</p><p>  static final int DEFAULT_INITIAL_CAPACITY = 1 &lt;&lt; 4;</p></li><li><p>The load factor used when none specified in constructor.</p><p>  static final float DEFAULT_LOAD_FACTOR = 0.75f;</p></li><li><p>The bin count threshold for using a tree rather than list for a bin. Bins are converted to trees when adding an element to a bin with at least this many nodes.</p><p>  static final int TREEIFY_THRESHOLD = 8;</p></li><li><p>The bin count threshold for untreeifying a (split) bin during a resize operation.</p><p>  static final int UNTREEIFY_THRESHOLD = 6;</p></li><li><p>An instance of HashMap has two parameters that affect its performance: <em>initial capacity</em> and <em>load factor</em>. </p></li></ul><ul><li>The <em>capacity</em> is the number of buckets in the hash table, and </li><li>the initial capacity is simply the capacity at the time the hash table is created.<br>The <em>load factor</em>s a measure of how full the hash table is allowed to get before its capacity is automatically increased.<br>When the number of entries in the hash table exceeds the product of the load factor and the current capacity, the hash table is <em>rehashed</em> (that is, internal data structures are rebuilt) so that the hash table has approximately twice the number of buckets.</li></ul><ul><li>As a general rule, the default load factor (.75) offers a good tradeoff between time and space costs. </li></ul><ul><li>Higher values decrease the space overhead but increase the lookup cost (reflected in most of the operations of the HashMap class, including get and put).<br>The expected number of entries in the map and its load factor should be taken into account when setting its initial capacity, so as to minimize the number of rehash operations. </li><li>If the initial capacity is greater than the maximum number of entries divided by the load factor, no rehash operations will ever occur.</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> hashmap </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Product of Array Except Self</title>
      <link href="/2020/08/16/product-of-array-except-self/"/>
      <url>/2020/08/16/product-of-array-except-self/</url>
      
        <content type="html"><![CDATA[<p>Given an array <code>nums</code> of <em>n</em> integers where <em>n</em> &gt; 1, return an array <code>output</code> such that <code>output[i]</code> is equal to the product of all the elements of <code>nums</code> except <code>nums[i]</code>.</p><p><strong>Example:</strong></p><p><strong>Input:</strong><code>[1,2,3,4]</code><br><strong>Output:</strong><code>[24,12,8,6]</code></p><p>Problem link: <a href="https://leetcode.com/problems/product-of-array-except-self">Product of Array Except Self</a></p><p>Solution:</p><p>Let’s start with a brute-force solution.</p><pre><code>public int[] productExceptSelf(int[] nums) &#123;    int[] arr = new int[nums.length];    for(int i = 0; i &lt; nums.length; i++) &#123;        int mul = 1;        for (int j = 0; j &lt; nums.length; j++) &#123;            if(i == j) &#123;                continue;            &#125;            mul *= nums[j];        &#125;        arr[i] = mul;    &#125;    return arr;&#125;</code></pre><p>Clearly, this is not an efficient solution and the time complexity for this solution is O(n²)</p><p>We can improve the implementation using the help of division. We can get the total multiplication value and for each element we divide the item.</p><pre><code>public int[] productExceptSelf(int[] nums) &#123;    int mul = 1;    for(int i = 0; i &lt; nums.length; i++) &#123;        mul *= nums[i];    &#125;        for (int i = 0; i &lt; nums.length; i++) &#123;        nums[i] = mul / nums[i];    &#125;    return nums;&#125;</code></pre><p>This solution seems okay. But we missed an important edge case here. The existence of zero(0). If there is any input 0, it will cause division by zero exception.</p><p>To solve the division by zero exception, we can think of two use cases</p><ol><li>One 0 exists in the input</li><li>0 exists more than once.</li></ol><p>For the use case for 0 existing more than once, we can easily define the result array will have all zeroes inside.</p><p>But, if we have only one 0, then we will only get output for that item only.</p><p><strong>Input:</strong><code>[1,2,0,4,5]</code><br><strong>Output:</strong><code>[0,0,40,0,0]</code></p><p>we can solve the problem using zero counter easily.</p><pre><code>public int[] productExceptSelf(int[] nums) &#123;    int mul = 1, countZero = 0;    for(int i = 0; i &lt; nums.length; i++) &#123;        if(nums[i] == 0) &#123;            countZero++;        &#125; else &#123;            mul *= nums[i];        &#125;    &#125;    if(countZero &gt; 1) &#123;        return new int[nums.length];    &#125;    for (int i = 0; i &lt; nums.length; i++) &#123;        if(countZero &gt; 0) &#123;            if(nums[i] == 0) nums[i] = mul;            else nums[i] = 0;        &#125; else &#123;            nums[i] = mul / nums[i];        &#125;    &#125;    return nums;&#125;</code></pre><p>There is a note in the problem</p><p>**Note: **Please solve it <strong>without division</strong> and in O(<em>n</em>).</p><p>To solve without division, we first need to find pattern of the problem. Let’s find it with provided example input.</p><p><strong>Input:</strong><code>[1,2,3,4]</code><br><strong>Output:</strong><code>[24,12,8,6]</code></p><p>We can distribute the input in left and right section, so that we can multiply left and right to get our result.</p><pre><code>Index: 0Nums[Index] = 1 (default)Left[Index] = 1Right[Index] = 2 * 3 * 4 = 24Result[Index] = Left[Index] * Right[Index] = 1 * 24 = 24Index: 1Nums[Index] = 2Left[Index] = 1Right[Index] = 3 * 4 = 12Result[Index] = Left[Index] * Right[Index] = 1 * 12 = 12Index: 2Nums[Index] = 3Left[Index] = 1 * 2 = 2Right[Index] = 4Result[Index] = Left[Index] * Right[Index] = 4 * 2 = 8Index: 3Nums[Index] = 4Left[Index] = 1 * 2 * 3 = 6Right[Index] = 1 (default)Result[Index] = Left[Index] * Right[Index] = 6 * 1 = 6</code></pre><p>To generate our left array</p><pre><code>left[i] = left[i - 1] * hums[i - 1]</code></pre><p>And our right array</p><pre><code>right[i] = right[i + 1] * hums[i + 1]</code></pre><p>So, our solution will be</p><pre><code>public int[] productExceptSelf(int[] nums) &#123;    int[] left = new int[nums.length];    left[0] = 1;    for (int i = 1; i &lt; nums.length; i++) &#123;        left[i] = left[i - 1] * nums[i - 1];    &#125;    int[] right = new int[nums.length];    right[nums.length - 1] = 1;    for (int i = nums.length - 2; i &gt;= 0; i--) &#123;        right[i] = right[i + 1] * nums[i + 1];    &#125;    for (int i = 0; i &lt; nums.length; i++) &#123;        nums[i] = left[i] * right[i];    &#125;    return nums;&#125;</code></pre><p>Please share any other better solution for this problem.</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> array </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Design a parking lot</title>
      <link href="/1970/01/01/design-a-parking-lot/"/>
      <url>/1970/01/01/design-a-parking-lot/</url>
      
        <content type="html"><![CDATA[<h2 id="System-Requirements"><a href="#System-Requirements" class="headerlink" title="System Requirements"></a>System Requirements</h2><p>We will focus on the following set of requirements while designing the parking lot:</p><ol><li>The parking lot should have multiple floors where customers can park their cars.</li><li>The parking lot should have multiple entry and exit points.</li><li>Customers can collect a parking ticket from the entry points and can pay the parking fee at the exit points on their way out.</li><li>Customers can pay the tickets at the automated exit panel or to the parking attendant.</li><li>Customers can pay via both cash and credit cards.</li><li>Customers should also be able to pay the parking fee at the customer’s info portal on each floor. If the customer has paid at the info portal, they don’t have to pay at the exit.</li><li>The system should not allow more vehicles than the maximum capacity of the parking lot. If the parking is full, the system should be able to show a message at the entrance panel and on the parking display board on the ground floor.</li><li>Each parking floor will have many parking spots. The system should support multiple types of parking spots such as Compact, Large, Handicapped, Motorcycle, etc.</li><li>The Parking lot should have some parking spots specified for electric cars. These spots should have an electric panel through which customers can pay and charge their vehicles.</li><li>The system should support parking for different types of vehicles like car, truck, van, motorcycle, etc.</li><li>Each parking floor should have a display board showing any free parking spot for each spot type.</li><li>The system should support a per-hour parking fee model. For example, customers have to pay $4 for the first hour, $3.5 for the second and third hours, and $2.5 for all the remaining hours.</li></ol>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
